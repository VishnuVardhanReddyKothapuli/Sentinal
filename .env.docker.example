# Copy to .env.docker and replace JWT_SECRET before starting Docker Compose.
JWT_SECRET=replace-with-at-least-32-random-private-characters
ENABLE_AI_MODELS=true
# CPU inference packages are installed by default. Set both false for API-only work.
BUILD_AI=true
