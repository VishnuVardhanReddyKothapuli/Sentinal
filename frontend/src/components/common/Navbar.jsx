import { Menu, ShieldCheck, ArrowUpRight } from 'lucide-react';
import { Link, useLocation } from 'react-router-dom';
import { useAuth } from '../../auth';
export default function Navbar({onMenu,health}){const {user}=useAuth();const location=useLocation();const section=location.pathname.startsWith('/analyze')?'Analysis studio':location.pathname==='/history'?'Analysis history':location.pathname==='/admin'?'Administration':location.pathname==='/login'?'Account access':'Overview';return <header className="topbar"><div className="breadcrumb"><button className="icon-button mobile-menu" onClick={onMenu} aria-label="Open navigation"><Menu size={21}/></button><span>Workspace</span><span className="slash">/</span><strong>{section}</strong></div><div className="top-actions"><span className={`connection ${health?'online':''}`}><span className="status-dot"/>{health?'API connected':'API unavailable'}</span>{user?<span className="top-account"><ShieldCheck size={16}/>{user.username}</span>:<Link to="/login">Sign in<ArrowUpRight size={15}/></Link>}</div></header>;}
