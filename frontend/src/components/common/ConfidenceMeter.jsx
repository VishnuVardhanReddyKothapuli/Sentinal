export default function ConfidenceMeter({value,label='Safe confidence'}){const hasValue=typeof value==='number';return <div className="confidence"><svg viewBox="0 0 160 160" role="img" aria-label={hasValue?`${label}: ${value.toFixed(1)} percent`:`${label}: not evaluated`}><circle className="dial-track" cx="80" cy="80" r="66"/><circle className="dial-value" cx="80" cy="80" r="66" strokeDasharray={`${hasValue?Math.max(0,Math.min(100,value))*4.147:0} 414.7`}/></svg><div><strong>{hasValue?value.toFixed(1):'—'}{hasValue&&<small>%</small>}</strong><span>{label}</span></div></div>;}
