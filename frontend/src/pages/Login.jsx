import { useState } from 'react';
import { Link, useLocation, useNavigate } from 'react-router-dom';
import { ArrowRight, Check, Eye, EyeOff, ShieldCheck } from 'lucide-react';
import { useAuth } from '../auth';
import { errorMessage } from '../api';
import { ErrorNotice } from '../components/common/Ui';
export default function Login(){const [register,setRegister]=useState(false);const [visible,setVisible]=useState(false);const [pending,setPending]=useState(false);const [error,setError]=useState('');const {authenticate,user}=useAuth();const navigate=useNavigate();const location=useLocation();const submit=async e=>{e.preventDefault();setError('');setPending(true);const form=new FormData(e.currentTarget);try{await authenticate(register?'register':'login',Object.fromEntries(form));navigate(location.state?.from||'/analyze/combined',{replace:true});}catch(err){setError(errorMessage(err));}finally{setPending(false);}};return <section className="auth-layout"><div className="auth-intro"><div className="auth-shield"><ShieldCheck size={48} strokeWidth={1.3}/></div><p className="eyebrow">BETTER SIGNALS. BETTER DECISIONS.</p><h1>Clarity starts<br/>with context.</h1><p>Your workspace for safer content and a more connected visual library.</p><ul>{['Analyze images and video in one place','See the evidence behind every assessment','Keep a searchable history of your decisions'].map(text=><li key={text}><Check size={17}/>{text}</li>)}</ul></div><div className="auth-card"><div className="auth-tabs" role="group" aria-label="Account access"><button className={!register?'selected':''} onClick={()=>{setRegister(false);setError('');}}>Sign in</button><button className={register?'selected':''} onClick={()=>{setRegister(true);setError('');}}>Create account</button></div><h2>{register?'Your workspace awaits.':'Welcome back.'}</h2><p>{register?'Create your Sentinel account to get started.':'Sign in to continue your content analysis.'}</p>{user?<><div className="notice">You’re signed in as {user.username}.</div><Link className="button primary full-width" to="/analyze/combined">Open analysis studio<ArrowRight size={17}/></Link></>:<form onSubmit={submit}><label htmlFor="username">Username</label><input id="username" name="username" autoComplete="username" required minLength={3} maxLength={50} placeholder="Your username"/>{register&&<><label htmlFor="email">Email address</label><input id="email" name="email" type="email" autoComplete="email" required maxLength={100} placeholder="you@company.com"/></>}<label htmlFor="password">Password</label><div className="password-field"><input id="password" name="password" type={visible?'text':'password'} autoComplete={register?'new-password':'current-password'} required minLength={register?12:1} maxLength={128} placeholder={register?'At least 12 characters':'Your password'}/><button type="button" className="icon-button" onClick={()=>setVisible(!visible)} aria-label={visible?'Hide password':'Show password'}>{visible?<EyeOff size={18}/>:<Eye size={18}/>}</button></div><ErrorNotice>{error}</ErrorNotice><button type="submit" className="button primary full-width" disabled={pending}>{pending?'Connecting…':register?'Create account':'Sign in'}<ArrowRight size={17}/></button></form>}<div className="auth-note"><ShieldCheck size={15}/><span>Analysis records are scoped to your account.</span></div></div></section>;}
